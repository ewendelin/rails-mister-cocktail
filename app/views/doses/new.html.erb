<h2>Create a new dose</h2>
<%= simple_form_for [@cocktail, dose_path] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredients %>
  <%= f.submit "Add new", class: "btn btn-primary" %>
<% end %>

